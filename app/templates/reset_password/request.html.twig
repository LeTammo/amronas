{% extends 'base.html.twig' %}

{% block body %}
    <div class="text-center">
        <h1 class="font-handwritten">Passwort zurücksetzen</h1>
        <div class="d-flex justify-content-center">
            {{ form_start(requestForm) }}
            {{ form_row(requestForm.email) }}
            {{ form_errors(requestForm.email) }}
            <button class="btn btn-primary">Abschicken</button>
            {{ form_end(requestForm) }}
        </div>
        <div class="mt-3">
            <p>Bitte gib deine E-Mail-Adresse ein,<br>um dein Passwort zurückzusetzen.</p>
            <p>Du erhältst dann eine E-Mail mit einem Link,<br>über den du ein neues Passwort festlegen kannst.</p>
        </div>
    </div>
{% endblock %}