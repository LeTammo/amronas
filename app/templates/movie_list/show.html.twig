{% extends 'base.html.twig' %}

{% block title %}MovieList{% endblock %}

{% block body %}
    <h1>MovieList</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ movieList.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ movieList.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_movie_list_index') }}">back to list</a>

    <a href="{{ path('app_movie_list_edit', {'id': movieList.id}) }}">edit</a>

    {{ include('movie_list/_delete_form.html.twig') }}
{% endblock %}
